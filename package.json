{
  "name": "eaasgrid-platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "echo 'Use individual app dev commands'",
    "build": "echo 'Build handled per app'",
    "start": "echo 'Start handled per app'"
  },
  "devDependencies": {
    "turbo": "^2.10.3",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "ws": "^8.21.0"
  }
}
